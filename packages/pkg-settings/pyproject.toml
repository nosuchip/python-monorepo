[project]
name = "pkg-settings"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pydantic-settings>=2.7.1",
]

[tool.hatch.build.targets.wheel]
packages = ["src/pkg_settings"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "app2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.8",
    "pkg-requests",
    "uvicorn>=0.34.0",
]

[tool.uv.sources]
pkg-requests = { workspace = true }
